const Footer = () => {
    return (
        // Use lowercase `footer` for the HTML element
        <footer>
          {/* Footer content goes here */}
          <p>My Footer</p>
        </footer>
      );
};

export default Footer;
